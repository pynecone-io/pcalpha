"""An image component."""

from pynecone.components.chakra import ChakraComponent


class Icon(ChakraComponent):
    """The Avatar component is used to represent a user, and displays the profile picture, initials or fallback icon."""

    tag = "Icon"
